import   itertools
from  a  import   (x ,  y,)
class ClassA:

   def method1(self,arg1) :
      

      return   "a"

# comment between 2 blank lines

def function1(arg1, arg2):
   return 2

def function2_with_a_really_long_name(and_several, parameters, that,           are, leading, to, quite, a, long, parameter,   list):
  very_long_variable_name =a_function_that_doesnt_exist_but_has_a_long_name(called, with_a, bunch_of,          arguments)
